# 🚀 Что делать после подключения к Vercel

## Шаг 1: Добавьте переменную окружения (ОБЯЗАТЕЛЬНО!)

1. **В панели Vercel** откройте ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **"Add New"**
4. Заполните:
   - **Name**: `VITE_API_URL`
   - **Value**: `http://159.89.99.252:8080/api/v1` (ваш API URL)
   - **Environment**: Отметьте все три:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development
5. Нажмите **"Save"**

## Шаг 2: Передеплойте проект

После добавления переменной окружения:

1. Перейдите на вкладку **"Deployments"**
2. Найдите последний деплой
3. Нажмите на три точки (⋮) → **"Redeploy"**
4. Или просто сделайте новый push в GitHub:
   ```bash
   git commit --allow-empty -m "Trigger redeploy"
   git push
   ```

## Шаг 3: Проверьте деплой

1. Дождитесь завершения деплоя (1-2 минуты)
2. Нажмите на **"Visit"** или откройте URL вида:
   `https://magazin-app-xxxxx.vercel.app`
3. Проверьте:
   - ✅ Сайт открывается
   - ✅ Все страницы работают
   - ✅ API запросы проходят (попробуйте зарегистрироваться или войти)

## Шаг 4: Проверьте логи (если что-то не работает)

1. В панели Vercel → **Deployments**
2. Выберите деплой → **"View Function Logs"**
3. Проверьте ошибки

## ⚠️ Важные моменты

### Проблема с CORS
Если API запросы не проходят, нужно настроить CORS на бэкенде:

```javascript
// На вашем API сервере (159.89.99.252:8080)
app.use(cors({
  origin: [
    'https://your-project.vercel.app', // ваш Vercel URL
    'http://localhost:3000' // для локальной разработки
  ],
  credentials: true
}));
```

### Проблема с HTTPS/HTTP
Если ваш API на `http://` (не HTTPS), браузер может блокировать запросы с HTTPS сайта.

**Решения:**
1. Настройте HTTPS для API (рекомендуется)
2. Или используйте прокси через Vercel (сложнее)

## ✅ Готово!

После этих шагов ваш сайт будет:
- 🌐 Публично доступен по URL Vercel
- 🔒 С автоматическим HTTPS
- 🚀 Автоматически обновляться при каждом push в GitHub

## Следующие шаги (опционально)

1. **Настройте кастомный домен** (если есть):
   - Settings → Domains → Add Domain

2. **Настройте автоматический деплой**:
   - Уже работает! Каждый push в `main` ветку = автоматический деплой

3. **Проверьте производительность**:
   - Vercel автоматически оптимизирует сайт

---

**Нужна помощь?** Проверьте логи деплоя в панели Vercel!
