# 🧪 Тестирование PWA

## Как проверить, что PWA работает:

### 1. Проверка в браузере (Chrome/Edge):

1. Откройте сайт на Vercel
2. Нажмите F12 (DevTools)
3. Перейдите в **Application** → **Manifest**
   - Должен быть виден манифест с иконками
   - Проверьте, что иконки загружаются (зеленые галочки)

4. Перейдите в **Application** → **Service Workers**
   - Должен быть зарегистрирован Service Worker
   - Статус: "activated and is running"

5. Перейдите в **Application** → **Storage**
   - Проверьте Cache Storage (должны быть кеши)

### 2. Проверка установки на мобильном:

#### Android (Chrome):
1. Откройте сайт на телефоне
2. В меню браузера (три точки) найдите **"Добавить на главный экран"** или **"Установить приложение"**
3. Нажмите и подтвердите установку
4. Иконка появится на главном экране
5. Запустите приложение - должно открыться в полноэкранном режиме

#### iOS (Safari):
1. Откройте сайт в Safari
2. Нажмите кнопку "Поделиться" (квадрат со стрелкой)
3. Выберите **"На экран «Домой»"**
4. Подтвердите добавление
5. Иконка появится на главном экране

### 3. Проверка через Lighthouse:

1. Откройте DevTools (F12)
2. Вкладка **Lighthouse**
3. Выберите **"Progressive Web App"**
4. Нажмите **"Analyze page load"**
5. Должен быть высокий балл (90+)

### 4. Что должно работать:

✅ **Манифест загружается** - `/manifest.webmanifest` доступен  
✅ **Service Worker активен** - виден в DevTools  
✅ **Иконки загружаются** - `icon-192.png` и `icon-512.png` доступны  
✅ **Установка доступна** - кнопка установки появляется на мобильных  
✅ **Офлайн работа** - базовые страницы работают без интернета  

## Возможные проблемы:

### Проблема: Не появляется кнопка установки

**Решение:**
- Убедитесь, что сайт открыт по HTTPS (Vercel предоставляет автоматически)
- Проверьте, что Service Worker зарегистрирован
- Очистите кеш браузера и перезагрузите страницу

### Проблема: Иконки не загружаются

**Решение:**
- Проверьте, что файлы `icon-192.png` и `icon-512.png` находятся в `public/`
- Проверьте пути в манифесте
- Убедитесь, что файлы закоммичены и задеплоены

### Проблема: Service Worker не регистрируется

**Решение:**
- Проверьте консоль браузера на ошибки
- Убедитесь, что `registerSW.js` загружается
- Проверьте, что сайт открыт по HTTPS

## Быстрая проверка:

Откройте консоль браузера (F12) и выполните:

```javascript
// Проверка Service Worker
navigator.serviceWorker.getRegistrations().then(regs => {
  console.log('Service Workers:', regs.length)
  regs.forEach(reg => console.log('SW:', reg.scope, reg.active?.state))
})

// Проверка манифеста
fetch('/manifest.webmanifest')
  .then(r => r.json())
  .then(m => console.log('Manifest:', m))
  .catch(e => console.error('Manifest error:', e))
```

## После установки проверьте:

1. ✅ Иконка на главном экране
2. ✅ Запуск в полноэкранном режиме (без адресной строки)
3. ✅ Работа офлайн (базовые страницы)
4. ✅ Быстрый доступ

Если все работает - PWA настроен правильно! 🎉
